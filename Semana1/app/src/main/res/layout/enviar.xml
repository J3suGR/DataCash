<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="20dp">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTituloEnviar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enviar dinero"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="16dp"
        android:textColor="#0D47A1" />

    <!-- Instrucción -->
    <TextView
        android:id="@+id/tvInstruccion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Completa los datos del destinatario"
        android:textSize="16sp"
        android:textColor="#212121"
        android:layout_marginBottom="16dp" />

    <!-- Destinatario -->
    <TextView
        android:id="@+id/tvDestinatario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="👤 Ingresar usuario"
        android:textSize="16sp"
        android:background="#E3F2FD"
        android:padding="12dp"
        android:layout_marginBottom="12dp"
        android:textColor="#000000" />

    <!-- Monto -->
    <TextView
        android:id="@+id/tvMontoEnviar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💵 Ingresar monto a enviar"
        android:textSize="16sp"
        android:background="#FFF9C4"
        android:padding="12dp"
        android:layout_marginBottom="20dp"
        android:textColor="#000000" />

    <!-- Botones de accesos rápidos de monto -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="20dp">

        <Button
            android:id="@+id/btnMonto10"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="S/ 10"
            android:background="#4CAF50"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btnMonto20"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="S/ 20"
            android:background="#4CAF50"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btnMonto50"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="S/ 50"
            android:background="#4CAF50"
            android:textColor="#FFFFFF" />
    </LinearLayout>

    <!-- Botón enviar -->
    <Button
        android:id="@+id/btnConfirmarEnvio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirmar Envío"
        android:background="#0D47A1"
        android:textColor="#FFFFFF" />

</LinearLayout>
